- `yarn`
- `yarn dev`

Results in:

```
[plugin vite:dep-scan] Failed to resolve entry for package "@cognite/intercom-helper". The package may have incorrect main/module/exports specified in its package.json: Failed to resolve entry for package "@cognite/intercom-helper". The package may have incorrect main/module/exports specified in its package.json.
```